# TODO

## Features

- [ ] Process filtering/search — type to filter process list by name
- [ ] Export/snapshot data to file (CSV or JSON)
- [ ] Config file support — persist default theme, refresh rate, units
- [ ] Process tree view — parent-child relationships
- [ ] Alert thresholds — highlight when swap usage exceeds a configurable percentage
